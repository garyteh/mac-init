- input source
- setup ssh keys
- install mas
- install BetterSnapTools
- install Amphetamine
- install LastPass
- install powerline
- install powerline-fonts
- switch to latest bash
- accessibility
- install "The Unarchiver"
- install java
- install jenv
    - setup jenv
- install sdkman
- determine if Application is installed (not only via brew cask)
- options to opt-out phases
- https://serverfault.com/a/663048
- skip and show warnings to formulas that does not exist anymore
- LOG file does not work
	- should print the LOG file location even it exit unexpectedly
- pip will not be found because it's not in the PATH yet
- will break .prompt
